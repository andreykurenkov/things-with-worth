package edu.gatech.GradeAwesome;

//TODO:again, not certain as to needs for generics or better way to do this
public interface Observable<TypeObserver extends Observable<?>> {
	public void acceptObserver(Observer<TypeObserver> add);
}
